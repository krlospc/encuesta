<!DOCTYPE html>
<html lang="es">
{% include 'AdmMaster/menuforms.html.twig' %}
<head>
	<style type="text/css">
		table {
			border-collapse: collapse;
		}
		th, td {
			border: 1px solid #000000;
			padding: 10px;
			text-align: left;
		}

	</style>
	<meta charset="UTF-8">
	<title>Formulario 2</title>
	<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
</head>
<body>
	<div class="container">
		<h1>FORMULARIO 2</h1>
		<a href="{{path('formulario2_new')}}" class="btn btn-success">Nuevo formulario</a>
		<table class="table-bordered" style="border:1px solid black;border-collapse:collapse; width:100%" >
			<tr>
				<th>Docente</th>
				<th>Cod. UE</th>
				<th>Unidad Educativa</th>

				<th>Operaciones</th>
			</tr>
            {% for docente in docentes %}
			<tr>
					<td>{{docente.p2Nombres}}  {{docente.p3Paterno}}  {{docente.p4Materno}}</td>
					<td>{{docente.codigoUnidadEducativa}}</td>
					<td>{{docente.p1Unidadeducativa}}</td>
					<td>
						<a href="{{ path('formulario2_edit', { 'id': docente.id }) }}" class="btn btn-primary">editar</a>
						<a href="{{ path('formulario2_delete', { 'id': docente.id }) }}" class="btn btn-danger" onclick="return confirm('Esta seguro de que desea eliminar el registro?')">eliminar</a>
					</td>

			</tr>
            {%endfor %}

		</table>





		<ul>

		</ul>
	</div>
	
</body>
</html>